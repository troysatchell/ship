# `platform/scopes/`

`ScopeRegistry` — scopes as data (PLUGFORGE.MD §2.3, §2.1; Epic E1 — PF-107).
New scopes register at module load; middleware is never edited to add one.
Empty until PF-107 lands.
