# Requirements gaps — Ship / PlugForge W6 (2026-08-16T14:40Z, commit 08505d2d)

Sweep `w6-2026-08-16b` (compare vs 08-16 07:10Z). 14 gap rows: 2 MISSING, 12 PARTIAL (three of them settled by rulings I-05..I-08 at the end of the run). Verdict bar and evidence: `REPORT-W6-2026-08-16b.md`, `matrix.after-w6-2026-08-16b.json`.

## Unticketed requirements

"Unticketed" here means: no *open* ticket owns the remaining part. Rows whose remaining part IS owned by an open ticket are listed second so the PM can see both.

### A. No open ticket owns the remaining part

### W6-R31 — PARTIAL [ruling I-06]
- **Quote:** "ShipClient.authorizationCodeFlow() and ShipClient.deviceLogin() handle their flows end-to-end. Pluggable ITokenStore (in-memory, file, browser localStorage)."
- **Source:** GFA_Week_6_PlugForge.pdf, p.4
- **Meaning in code:** SDK auth helpers for both grants + ITokenStore with the three store implementations.
- **Tickets (all):** TRO-418, TRO-600, TRO-425, TRO-449
- **What is missing:** Ruled 2026-08-16 (I-06, face-value reading applied by the audit session under Troy's batch instruction): the brief lists localStorage as one of the SDK's pluggable stores, so the SDK must ship it — a consumer-side implementation does not satisfy the clause. Everything else in the row (deviceLogin, authorizationCodeFlow, Memory/File stores, TRO-600 atomic write) is green this sweep.
- **Suggested scope:** Export a browser-safe LocalStorageTokenStore from @ship/sdk (move/duplicate integrations/browser-demo/src/localStorageTokenStore.ts into sdk/src/, export from the browser barrel; browser-demo imports it from the SDK). ~20 min + a unit test; parity/size gates unaffected (zero deps).

### W6-R36 — PARTIAL [ruling I-05]
- **Quote:** "Every public API call recorded with timestamp, app client_id, user_id, route, scope used, status, latency. Queryable in the developer portal."
- **Source:** GFA_Week_6_PlugForge.pdf, p.4
- **Meaning in code:** public_api_audit table + recording middleware + portal query surface.
- **Tickets (all):** TRO-432, TRO-436, TRO-439, TRO-443
- **What is missing:** Ruled 2026-08-16 (I-05): "queryable in the developer portal" requires a portal UI view. Table + recording middleware + GET /api/v1/audit + SDK audit.list() are all VERIFIED-level (api audit.test.ts 12 ✓, sdk audit tests ✓); the portal has no audit page (grep of web/src for /api/v1/audit → nothing; nav = Apps + Webhooks). CORRECTION vs prior sweep, which inferred the portal surface from the portal existing.
- **Suggested scope:** Add an Audit page under /developer/* (DeveloperSidebar DEVELOPER_NAV entry) that calls GET /api/v1/audit via usePortalToken().callV1 with cursor pagination and an app_client_id filter; portal token already carries audit:read (DeveloperPortalContext.tsx:35). RTL test + optional e2e. No ticket exists — PM GO item 5.

### W6-R46 — PARTIAL
- **Quote:** "Create a webhook subscription via the SDK; create a document; verify a signed POST arrives at the target URL within 2s; verify the signature with the SDK helper; tamper with the body and verify the helper rejects it."
- **Source:** GFA_Week_6_PlugForge.pdf, p.5
- **Meaning in code:** Graded test scenario — end-to-end webhook happy path + tamper negative, ≤2s first delivery.
- **Tickets (all):** TRO-455, TRO-433, TRO-438, TRO-413, TRO-607, TRO-599
- **What is missing:** DOWNGRADE from prior VERIFIED, on re-reading the artifact: the prior row said ttfe.ts 'implements exactly this scenario'; `grep -n -i 'tamper' scripts/drill/ttfe.ts` returns nothing (exit 1) and the only latency assertion is the 15000ms wait_for_delivery stage budget (ttfe.ts:491, ttfe.config.json:9). Observed CI latencies (599ms, 690ms) DO satisfy the 2s number — but observed-once-in-a-log is not the 'latency assertion' the acceptance evidence names. Tamper rejection of the same pure verifyWebhook() function is proven at sdk/src/verifyWebhook.test.ts:111-117 (synthetic) and integrations/cli/src/commands/webhooksTail.test.ts:99 (wrong-secret, 401) — both passed this sweep. Implementing ticket: TRO-455 (Done). Constituent pieces: TRO-433 signer, TRO-438 deliverer, TRO-413 verifyWebhook, TRO-607/TRO-599 SDK request/response shape fixes that unblocked the drill (all Done). No open ticket covers the two missing checks.
- **Suggested scope:** Two ~10-line additions to scripts/drill/ttfe.ts: (1) after stage 6, flip one byte of captured.rawBody and assert verifyWebhook(...) === false (tamper negative on the REAL delivered payload); (2) assert the wait_for_delivery stage ms <= 2000 explicitly (or add a stageBudgetsMs entry of 2000 for it — note the config comment at ttfe.config.json:2 deliberately keeps stage budgets generous, so a separate assertion may be preferable to tightening the budget).

### W6-R52 — PARTIAL [ruling I-08]
- **Quote:** "Webhook delivery latency (P95, first attempt)|< 2 s"
- **Source:** GFA_Week_6_PlugForge.pdf, p.6
- **Meaning in code:** First-attempt delivery latency target.
- **Tickets (all):** TRO-438, TRO-455
- **What is missing:** DOWNGRADE with correction: the prior 08-16 row said 'P95<2s first-delivery-latency assertion now lives on main (scripts/drill/thresholds.ts)'. I read thresholds.ts (pure budget evaluator, no latency-specific logic) and ttfe.config.json (wait_for_delivery: 15000; verify_webhook: 2000). The 2000 figure belongs to the HMAC-verify stage (W6-R58's metric), not first-attempt delivery latency. So the prior VERIFIED over-credited; the 08-15 verdict (PARTIAL: latency recorded, target unasserted) was the accurate one and remains so. What IS present: latency_ms per attempt (deliverer.ts:694, tested at webhooks.test.ts:770), and the drill's wait_for_delivery timing observed at 690 ms in CI run 31950529207's predecessor 31949732432 (job 95172637326). Downstream: W6-R46 (not my cluster) carries a similar note ('signed POST arrives within 2s') — its cited drill has the same 15 s ceiling; the main session may want to re-check that row too. bearing-but-not-green: vitest ["scripts/drill/__tests__/thresholds.test.ts"] e2e-red [] e2e-not-run []
- **Suggested scope:** Compute and assert a real P95: (1) record wait_for_delivery ms per drill run as a CI artifact/output; (2) a small script (scripts/drill/p95.mjs, gh-fed or artifact-fed) that aggregates the last ≥20 drill-ttfe runs and fails if P95 ≥ 2000 ms; run it in the drill-ttfe job. Per-run ceilings alone do not satisfy the face-value P95 target (I-08).

### W6-R55 — PARTIAL [ruling I-07]
- **Quote:** "CLI drill harness: pnpm drill ttfe runs the full loop end-to-end against a containerized Ship instance from a clean working directory."
- **Source:** GFA_Week_6_PlugForge.pdf, p.6
- **Meaning in code:** The `pnpm drill ttfe` command with containerized Ship (testcontainers per repo pattern).
- **Tickets (all):** TRO-455
- **What is missing:** Ruled 2026-08-16 (I-07): "containerized Ship instance" means the Ship API runs from a container image. Today only Postgres is containerized; the API is spawned via tsx from the checkout (ttfe.ts:185-203). Clean-dir SDK install, real HTTP against a real Ship, 30 consecutive green CI runs — all real, but the API-in-image half is absent.
- **Suggested scope:** Add an opt-in DRILL_TTFE_API_IMAGE=<ghcr image> path in scripts/drill/ttfe.ts that starts the built Ship image via testcontainers GenericContainer instead of spawning tsx, and run it in ci.yml's drill-ttfe job (GitHub only — GitLab's shared runner cannot start containers, .gitlab-ci.yml:233-241). Image already built by ci.yml:597 build-image. 2–3 h.

### W6-R64 — PARTIAL
- **Quote:** "LLM API spend during the agent rewire (Epic 7) — track per-day spend while migrating direct service calls to SDK calls; confirm the rewire does not change token volume."
- **Source:** GFA_Week_6_PlugForge.pdf, p.9
- **Meaning in code:** Dev-cost tracking obligations (this plus CI minutes, Playwright compute, spec-gen overhead, delivery-log storage — same list, p.9).
- **Tickets (all):** TRO-434, TRO-440, TRO-605
- **What is missing:** Re-classified from IMPLEMENTED-UNVERIFIED to PARTIAL: the artifacts themselves state the acceptance claim ('rewire does not change token volume') is unconfirmed. Since the prior sweep, TRO-605 (Done) removed the SERVER-side reason for the divergence, but agent/src/shipClient.ts:548-563 still nulls content in sdk mode, so PF-704's traced gap stands. TRO-434 documents (Done); TRO-440 wrote the delta (Done); no open ticket owns the follow-up measurement or the PF-905 §2.1 backfill.
- **Suggested scope:** (1) Update agent/src/shipClient.ts getDocumentViaSdk to pass through content/visibility/created_by from the widened SDK Document (sdk/src/types.ts:102-115) and drop the fail-closed 'sdk_mode_unknown' synthesis where the server now supplies real values; update shipClientParity.liveServer.test.ts:333 accordingly. (2) Run the same on_demand workload in both AGENT_PLATFORM_MODE values, diff agent cost-ledger rows (cost-report.ts by day), commit the two ledger snapshots. (3) Replace PF-905 §2.1's TODO with the measured numbers and change api/src/__tests__/pf905CostAnalysisDocSections.test.ts:71 to assert the filled section instead of the placeholder.

### W6-R71 — PARTIAL
- **Quote:** "GitHub Repository Public; per-slice branches preserved; each PR description lists which acceptance criterion that slice advances and confirms the fitness test passed."
- **Source:** GFA_Week_6_PlugForge.pdf, p.12
- **Meaning in code:** Repo/PR process discipline for the week.
- **Tickets (all):** none
- **What is missing:** Public + branch-preservation halves observed live via gh this sweep. PR-description mandate: 1 of 4 sampled recent merged PRs is literally compliant. No PR template in the repo to enforce it. Side observation: `gh run list` showed a newer GitHub-main head (a3ef8ee7, run 31950529207 in progress at 13:41Z) than this checkout's 08505d2d — main session may want to re-sync; not a verdict input.
- **Suggested scope:** Add .github/PULL_REQUEST_TEMPLATE.md with 'Acceptance criterion advanced:' and 'Fitness test passed:' fields (retro-fit not required by the brief).

### W6-R75 — PARTIAL
- **Quote:** "Before → fix → after → proof. For Epic 6, proof is the TTFE drill passing in CI. For Epic 7, proof is the agent's audit-log rows showing OAuth app authentication."
- **Source:** GFA_Week_6_PlugForge.pdf, p.13
- **Meaning in code:** Per-epic write-ups with the two named proofs.
- **Tickets (all):** TRO-437, TRO-455, TRO-440
- **What is missing:** Downgraded from IMPLEMENTED-UNVERIFIED after reading the doc against the two named proofs: (1) E6 — observed via `gh run view` this sweep that the CI job 'drill · TTFE (PF-603)' concluded success on main runs 31935025680 (08:00Z) and 31949732432 (13:37Z, 2026-08-16), so the CI proof exists but the write-up cites only a local run; (2) E7 — section absent (:14) though TRO-440 is Done with PF-704-COST-LEDGER-DELTA.md + auditTrailProof.liveServer.test.ts. E5 is also absent (:10) but the brief names no E5 proof. TRO-437 implements; TRO-455/TRO-440 produced the two named proofs.
- **Suggested scope:** Update PLUGFORGE-EPIC-WRITEUPS.md: add the E7 section citing auditTrailProof.liveServer.test.ts's printed audit rows / PF-704-COST-LEDGER-DELTA.md, and cite a green CI run ID for the drill-ttfe job in E6 (e.g. run 31949732432).

### B. An open ticket already owns the remaining part

### W6-R10 — PARTIAL (open ticket(s): TRO-613, TRO-549)
- **Quote:** "Existing Playwright regression suite passes on main; P95 latency, bundle size, and per-route query counts within +10% of the Part 1 baseline."
- **Source:** GFA_Week_6_PlugForge.pdf, p.2
- **Meaning in code:** No regression: e2e suite green, and the three Part-1 baseline metrics (in `audit/`) stay within +10%. (MVP hard gate.)
- **Tickets (all):** TRO-593, TRO-594, TRO-595, TRO-596, TRO-609, TRO-613, TRO-549, TRO-601, TRO-602
- **What is missing:** Since the prior sweep: TRO-609 merged (PR #266) — the sprint-filter cascade that skipped 30 tests is fixed; that same PR filed TRO-613 (In Progress) for the next failure in file order, so program-mode-week-ux.spec.ts is still known-red on main. TRO-549 (In Progress) is an assertion-weakness ticket, not a failure. Observed (main session's targeted e2e run this sweep, 7 platform specs incl. the two portal specs): 12 passed / 2 flaky-then-passed — a subset, not the regression suite. `pnpm test` (vitest, this sweep) completed 124/125 files passed; the single failure is api/src/__tests__/activity.test.ts (unrelated to this row) — main session reconciles. Numeric compares are stale by 2 days but PLUGFORGE's own gate methodology treats them as point-in-time; no code merged since 08-14 touches bundle-heavy or hot-path routes per the CHANGES.md entries read (derived, not re-measured). bearing-but-not-green: vitest [] e2e-red [] e2e-not-run ["e2e/session-timeout.spec.ts","e2e/accessibility-remediation.spec.ts","e2e/admin-workspace-members.spec.ts","e2e/program-mode-week-ux.spec.ts"]
- **Suggested scope:** Close TRO-613, then capture one whole-suite Playwright run on current main (summary.json committed under audit/) and re-run the three compares against that commit.

### W6-R12 — PARTIAL (open ticket(s): TRO-415)
- **Quote:** "Terraform deployment: a terraform/ directory with a complete config describing the deployment topology (app container, database, networking, IAM task role and execution role). Provider versions must be pinned. Run terraform plan and include the annotated output as a submission artifact. Perform a destroy-and-redeploy: tear down the environment and re-apply from the Terraform config alone to prove IaC completeness."
- **Source:** GFA_Week_6_PlugForge.pdf, p.2
- **Meaning in code:** Complete IaC for the deployment (repo deploys to Render — the IAM-role language is AWS-shaped and needs adaptation), pinned providers, committed annotated plan, destroy-redeploy evidence. (MVP hard gate.)
- **Tickets (all):** TRO-411, TRO-488, TRO-420, TRO-415
- **What is missing:** TRO-411 implements; TRO-488 hardens (validation blocks; verified via `terraform validate` per CHANGES.md:156 and PR #273 evidence table — observed from PR body, not re-run here: no terraform binary in this environment); TRO-420 supplies the IAM adaptation memo; TRO-415 (destroy-redeploy of ship/ship-db) is Backlog. Ran scripts/factory/verify-terraform-artifact.sh against the committed plan this sweep: all 12 mechanical checks PASS (string-presence only; does not prove the plan is clean against live state — the script says so itself). AWS-root terraform/ (versions.tf:7 `~> 5.0`) is a separate, non-graded deployment; per I-01's precedent the exercise directory is what counts.
- **Suggested scope:** TRO-415: obtain the human go-ahead, then run terraform destroy + apply for render_web_service.ship and render_postgres.ship, committing the log under terraform/render/plan/.

### W6-R40 — PARTIAL (open ticket(s): TRO-415)
- **Quote:** "Demonstrate drift: manually change a resource, run terraform plan, show the detected diff. Perform terraform destroy then terraform apply from scratch. Submit screenshots or log output proving the service came back up identically."
- **Source:** GFA_Week_6_PlugForge.pdf, p.5
- **Meaning in code:** Drift-detection demo + destroy-redeploy with committed evidence.
- **Tickets (all):** TRO-415
- **What is missing:** No new evidence since prior sweep. Drift demo: real terraform runs, committed raw logs, but local provider (W4 Category-8 fixture). Destroy-redeploy: real, cloud, observed health checks — but for render_web_service.agent only, and 'identically' does not hold (new service ID/URL, disclosed at proof line 23). TRO-415 Backlog with ⚠️ HUMAN GO-AHEAD.
- **Suggested scope:** TRO-415: manual drift on a live ship env var → `terraform plan` diff captured; then destroy + apply of ship/ship-db with before/after /health logs — committed under terraform/render/plan/.

### W6-R72 — PARTIAL (open ticket(s): TRO-444, TRO-429)
- **Quote:** "The five-line story is the demo: open a fresh terminal → pnpm install @ship/sdk → ship login → ship docs create → ship webhooks tail produces a verified signed delivery. Then switch to the dev portal and replay one delivery."
- **Source:** GFA_Week_6_PlugForge.pdf, p.12
- **Meaning in code:** Demo video (3–5 min) with that exact narrative.
- **Tickets (all):** TRO-444, TRO-429, TRO-452, TRO-448, TRO-450
- **What is missing:** Re-tiered MISSING→PARTIAL strictly by the tier rule (acceptance evidence = 'recorded video + committed script'; a committed script exists), not because new evidence appeared. Grep for .mp4/loom.com/youtu in docs/submission/*.md and README.md: zero hits. Dependencies all Done: TRO-448 (ship login), TRO-450 (ship docs), TRO-452 (ship webhooks tail), TRO-439 (portal replay). The recording and a script that actually follows the five-line story are the gap.
- **Suggested scope:** TRO-444/TRO-429: rewrite the script's core act around the literal five-line CLI story + portal replay, record the 3–5 min video, commit the script and link the video.

### W6-R73 — MISSING (open ticket(s): TRO-429)
- **Quote:** "All three phases completed with written answers; saved AI conversation attached as a reference artifact."
- **Source:** GFA_Week_6_PlugForge.pdf, p.13
- **Meaning in code:** Pre-Search document (Appendix phases 1–3) with real answers + the saved conversation artifact.
- **Tickets (all):** TRO-429
- **What is missing:** Searched: `grep -ril 'pre-search\|presearch' docs PLUGFORGE.MD *.MD *.md` (hits only PRESEARCH.MD, PLUGFORGE.MD, the demo script's reference to it, and CHANGES.md's TRO-360 W5 entry) and `find . -iname '*conversation*' -o -iname '*presearch*'` (only PRESEARCH.MD). Nothing W6-specific.
- **Suggested scope:** TRO-429: create PRESEARCH-W6.MD (or docs/submission/PLUGFORGE-PRESEARCH.md) with the three phases answered by Troy, plus the exported AI conversation file.

### W6-R78 — MISSING (open ticket(s): TRO-444)
- **Quote:** "Tag @GauntletAI. The screenshot is the ship webhooks tail terminal showing a verified signed event arriving in real time."
- **Source:** GFA_Week_6_PlugForge.pdf, p.13
- **Meaning in code:** Social post deliverable with the specified screenshot.
- **Tickets (all):** TRO-444, TRO-452
- **What is missing:** grep -i gauntletai across docs/submission and README: hits only in the W4/W5 drafts and their asset README. social-assets/ contains 5 W4 PNGs + w4-w5/ 12 PNGs; no terminal screenshot of webhooks tail.
- **Suggested scope:** TRO-444: run `ship webhooks tail` against the live deploy, capture the '✓ verified' terminal frame, and commit docs/submission/PLUGFORGE-SOCIAL-POST.md + the PNG.

## Observations worth a ticket (not brief-line gaps)
- `sdk/src/types.ts:170` — `IssuePriority` union lacks `'none'` after PR #276 (TRO-501) widened `shared` + OpenAPI; SDK↔spec parity walks operations, not enum members, so it passes. Small SDK type fix + a parity check on enum members.
- `integrations/slack/src/server.test.ts` (6 tests) is wired into no runner or CI job — only type-checked. Add `test:slack` to the root chain and CI.
- Two portal e2e assertions flaked on first attempt this sweep (`developer-portal-apps.spec.ts:119` networkidle race; `developer-portal-dlq-replay.spec.ts` `getByLabel(/^app$/i)` timeout) — both are the point-in-time async checks `e2e/AGENTS.md` forbids.
- `README.md:387` still says the developer portal "doesn't exist yet".
- `agent/src/shipClient.ts:548-563` nulls `content` in sdk mode though the server now returns it (TRO-605) — the traced cause of W6-R64's unconfirmed token-volume claim.
- `api/src/__tests__/activity.test.ts` failed once in a full run under load and passed 2/2 isolated — TEST-12 flake class, internal route.

## Orphan tickets
- TRO-612 (Done) "webhooks.liveServer.test.ts: oauthAppId setup guard should fail loudly, not skip" — maps to no inventory requirement.
- TRO-610 (Done) "Absolute session-timeout "I Understand" button silently extends the session it says cannot be extended" — maps to no inventory requirement.
- TRO-608 (Duplicate) "GHCR image build broken: webhooks.test.ts imports across Docker build-context boundary into docs/" — maps to no inventory requirement.
- TRO-606 (Duplicate) "GHCR image-build check broken on main — TRO-447/PF-801's test imports docs/submission/ outside the Docker build context" — maps to no inventory requirement.
- TRO-587 (Done) "CodeQL js/insufficient-password-hash false-positive on oauth/credentials.ts — needs formal dismissal" — maps to no inventory requirement.
- TRO-490 (Backlog) "Pre-existing: swagger.ts YAML converter emits mis-indented YAML (openapi.yaml) — JSON spec unaffected" — maps to no inventory requirement.
- TRO-400 (Done) "PF-201: Issues, sprints, me — typed views over the unified document model" — maps to no inventory requirement.
- TRO-395 (Backlog) "EPIC E9 — Infrastructure, docs, submission (PF-900–908)" — epic-parent organizational issue — maps to no single requirement.
- TRO-394 (Backlog) "EPIC E8 — Reference integrations (PF-800–805)" — epic-parent organizational issue — maps to no single requirement.
- TRO-393 (Backlog) "EPIC E7 — Agent as platform citizen 🔔 (PF-700–704)" — epic-parent organizational issue — maps to no single requirement.
- TRO-392 (Backlog) "EPIC E6 — CLI + TTFE drill (PF-600–603)" — epic-parent organizational issue — maps to no single requirement.
- TRO-391 (Backlog) "EPIC E5 — Rate limiting, audit, portal (PF-500–504)" — epic-parent organizational issue — maps to no single requirement.
- TRO-390 (Backlog) "EPIC E4 — @ship/sdk (PF-400–405)" — epic-parent organizational issue — maps to no single requirement.
- TRO-389 (Backlog) "EPIC E3 — Webhooks (PF-300–306)" — epic-parent organizational issue — maps to no single requirement.
- TRO-388 (Backlog) "EPIC E2 — Public resources & OpenAPI (PF-200–205)" — epic-parent organizational issue — maps to no single requirement.
- TRO-387 (Backlog) "EPIC E1 — OAuth core 🔔 (PF-100–107)" — epic-parent organizational issue — maps to no single requirement.
- TRO-386 (Backlog) "EPIC E0 — Boundary & error contract (PF-001–004)" — epic-parent organizational issue — maps to no single requirement.

## ship-pm decision
See `REPORT-W6-2026-08-16b.md` § "ship-pm scope-gate decision" — GO items 1–4 (drill asserts, write-ups E7 + README line, PR template, SDK IssuePriority), post-ruling GO items 5–7 (portal Audit page, SDK LocalStorageTokenStore export, image-backed drill path — the last is not a today item), NOT-NOW 8–10, HUMAN escalations (TRO-429/444/415). Tickets to be filed by the factory session (this audit session does not write to Linear).
