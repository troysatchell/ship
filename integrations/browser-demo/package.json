{
  "name": "@ship/browser-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Reference integration (PF-802, PLUGFORGE.MD §4): browser SPA using @ship/sdk's authorizationCodeFlow() PKCE, no secret in the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "seed-oauth-app": "node scripts/seed-oauth-app.mjs"
  },
  "dependencies": {
    "@ship/sdk": "workspace:*"
  },
  "devDependencies": {
    "pg": "8.16.3",
    "typescript": "5.9.3",
    "vite": "6.4.1"
  }
}
