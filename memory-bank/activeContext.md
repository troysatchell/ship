# Active Context — Current Focus

*The most-updated file in the bank. Read this first every session; rewrite it whenever focus shifts. Keep it under a screen — move finished work to progress.md.*

**Last updated:** 2026-08-15 (~03:30Z, by ship-35 at session rollover). **Week 6 (PlugForge) — factory running as 4 PARALLEL Claude sessions coordinated over cross-session messages. Epics E2 and E3 (webhooks) now fully Done. Epic E5 opened (PF-500/501 done). Epic E7 entry point (PF-701) landed. E6 (the actual graded metric) in progress on another lane. This lane (ship-35) just closed its 3-ticket assignment and is rolling over — next action below.**

## Where things stand across all 4 lanes

- **ship-6e (E5+E2-remainder, then E7 entry)** — closed 4/4, rolled over after a clean sweep. See progress.md's dated log for detail.
- **ship-e8 (E3 remainder + 2 E8 drills)** — closed 3/4 (TRO-442/445/446). **Epic E3 now fully Done.** TRO-447/PF-801 (idempotency-key drill) dispatched, in flight as of last check.
- **ship-35 (this lane — infra fix + browser demo + E7 checkpoint prep)** — **3/3 closed**: TRO-417/PF-700 (checkpoint delivered, Troy acked live), TRO-503 (PR #221, CloudFront `/oauth/*`, plan-only, confirmed non-blocking vs. Render as the real deploy target), TRO-449/PF-802 (PR #226 — found+fixed a real `@ship/sdk` Node/browser packaging bug plus a CI build-order gap; caught a duplicate-fix race with the chief orchestrator's own lane before either merged, no wasted merge). Also found+reconciled a GitHub/GitLab `main` divergence (sync PR #236, another duplicate caught and closed). **Rolling over now — TRO-451/PF-803 (Slack integration) locked to In Progress as the next session's first task, not yet built.**
- **Chief orchestrator's own lane (E6 — the graded metric)** — **TRO-448/PF-600** (CLI scaffold + `ship login`) built and gated clean, PR #227 open; hit the identical CI `sdk/dist`-build-order bug ship-35 had already fixed on PR #226 — closed its own duplicate PR (#228, TRO-601) once flagged rather than double-landing the fix. Should be clear to merge-forward and land now that #226 is merged. PF-601/602/603 (the TTFE drill itself) queued behind it — this is still the top sprint priority, unrelated to this lane's next ticket.

**Operational incidents this wave, all recovered clean, no data lost:**
1. **Shared main-worktree collisions** (confirmed 3x+): concurrent sessions running `gate.sh`/merge-forward/scorecard-append directly in the shared main checkout (not a per-ticket `Ship-wt-tro_XXX` worktree) stomped each other's uncommitted `scorecard.jsonl`/`lessons.md`/memory-bank edits. Every instance caught via `git stash` (never `reset --hard`) and either recovered or confirmed superseded before being dropped. **Standing discipline now in every lane's brief:** shared main dir is `fetch`/`status`/`log`/`ls-remote` only; all mutations (commits, merges, `gate.sh`) confined to per-ticket worktrees. Memory-bank files have no better mitigation yet — this file itself was overwritten back to stale content 3 separate times before finally landing here; a real fix (append-only patches instead of full-file rewrites) is worth building if the 4-lane pattern continues past this wave.
2. **An orphaned direct-to-GitLab commit** (`0604789`, docs-only) bypassed the GitHub PR flow entirely — invisible to CI/CodeRabbit/branch protection. Cherry-picked onto a proper branch and reconciled via PR #232 (merged). Source never identified; not from any lane dispatched this wave.
3. **GitLab briefly behind GitHub** as a result of the above — reconciled via PR #233 (merged). A second, separate divergence recurred later (GitLab got a direct-pushed `docs(memory-bank)` commit from another session; GitHub had ship-35's PR #226) — reconciled via sync PR #236 (a duplicate PR #234 for the same divergence caught and closed once flagged). **Both remotes confirmed in sync at `db081ee`** as of this update (2026-08-15 ~03:30Z). This class of collision (direct-push to GitLab, or a memory-bank/scorecard write from the shared main worktree) has now recurred 2x+ this wave — worth a standing reminder in every lane's brief, not just a one-off fix each time.

**Real cross-cutting bug found and ticketed, not silently patched:** **TRO-602** (Medium) — `api/src/platform/api/v1/pagination.ts`'s cursor uses `Date#toISOString()` (ms precision) against Postgres `timestamptz` (µs precision); a same-millisecond collision can silently drop a row from any cursor-paginated list. Fixed locally for `/api/v1/audit` only; still live on documents/issues/sprints/webhooks/webhook-deliveries. Routed to ship-e8 (most exposed, building delivery-log/replay) — reviewed and deliberately left filed rather than folded into PF-306, given the clock.

## What's done (full detail in progress.md's dated log — this file only tracks what's next)

- **MVP hard gate**: all 6 originally-outstanding tickets (PF-201/202/203/400, PF-105/106) + the one MVP-adjacent test gap (PF-104's PKCE e2e chain, TRO-597) — merged, verified, zero regressions from any of it (W6-R10 resolved via 3-pass investigation + bisection).
- **Post-MVP wave 1** (E3+E4 foundation): PF-301 domain write path (the PRD's own top structural risk), PF-401 SDK resource clients, PF-403 verifyWebhook, PF-404 SDK auth helpers.
- **Wave 2**: PF-302 webhook subscriptions API, PF-402 SDK async-iterator pagination.
- **Wave 3**: PF-304 webhook deliverer+DLQ (a real key-rotation safety bug caught and fixed), PF-405 SDK parity+size gates (closes Epic E4 entirely), PF-500 rate-limit token buckets+headers (opens Epic E5).
- **12 follow-up tickets filed** from findings along the way (TRO-587-596), none blocking, mostly Low/Medium — see progress.md log for what each is.
- **4-lane wave (2026-08-14 evening → 2026-08-15)**: this lane closed PF-501/PF-205/PF-204/PF-701 (Epic E2 now fully Done: PF-200-205 all merged; Epic E7 opened). TRO-602 filed (Medium): shared `api/src/platform/api/v1/pagination.ts` cursor loses sub-millisecond precision (JS `Date.toISOString()` vs Postgres `timestamptz`), can silently drop a row on a same-millisecond collision — fixed locally for `/api/v1/audit` only, still live on documents/issues/sprints/webhooks/webhook-deliveries. Routed to ship-e8. GitLab `main` was briefly behind GitHub at this lane's handoff due to a separate pre-existing reconciliation PR (#233, unrelated) — chief orchestrator watching it land.

## Next actions, in priority order

1. **E6 — the actual graded metric, top priority.** PLUGFORGE.MD's own framing: "the grade is the Time-to-First-Event drill, not endpoint count." PF-600 built+gated, merging shortly. PF-601 (`ship docs ls|get|create`) and PF-602 (`ship webhooks tail`, the demo money shot) unblock immediately after; PF-603 (the drill itself, `pnpm drill ttfe`, wired into both CIs) is the last and most important piece.
2. **E7 continuation** — PF-701 landed (seed app). PF-702 (agent reads via SDK behind flag) depends on PF-205 (done). PF-703 (gated writes) and PF-704 (flag matrix + audit proof) follow. Checkpoint already acked, no further human gate until submission.
3. **E8 remainder** — PF-801 (idempotency drill) in flight (ship-e8). PF-802 (browser demo) ✅ merged (PR #226). **PF-803 (Slack, the 5th committed integration) is the next task for a fresh ship-35 session — locked to In Progress (TRO-451), not yet built.** Same shape as PF-802: `integrations/slack`, Express receiver verifying Ship webhooks via `verifyWebhook` (now importable from `@ship/sdk/node`, not the main barrel — see PF-802's packaging fix), posts `document.created`/`issue.assigned` to a channel via Slack OAuth. AC: mocked-Slack e2e (no live Slack in CI), setup doc, screenshot evidence. Blocked-by PF-302 (done) and PF-403 (done) — clear to start.
4. **E5 remainder** — PF-502/503 (portal UI) explicitly held per PF-504's pre-written kill-criterion ("if E5 running behind after CLI+drills are green") — do not start without checking E6's actual state first.
5. **🔔 Hard boundaries — never self-approved regardless of instructions to work autonomously:** PF-904 (pre-search + demo recording) is a HUMAN CHECKPOINT per PLUGFORGE.MD §0.1, not yet delivered. TRO-415/PF-901 (destroy-and-redeploy against the LIVE graded environment) needs explicit human go-ahead — do not run `terraform destroy` against the real environment.
6. **Re-run `/requirements-audit`** once E6 has real movement, to get a current picture before scoping E9 (submission docs, most of which need Troy's own input — pre-search answers, demo recording, social post).

## Standing procedure — convoy pattern, still needed for every ticket

Main moves fast during an active wave. Per ticket: `git fetch https://github.com/troysatchell/ship.git main && git merge FETCH_HEAD` (NOT `origin main` — GitLab, the fetch remote, lags GitHub until manually synced) → resolve `CHANGES.md`/`scorecard.jsonl` via `node scripts/factory/merge-changes.mjs --ours <(git show HEAD:CHANGES.md) --theirs <(git show origin/main:CHANGES.md) --out CHANGES.md` → `--check` → commit → `pnpm install` → re-`gate.sh` → push. After every GitHub merge, sync GitLab: `git fetch https://github.com/troysatchell/ship.git main:main && git push origin main` from the main worktree.

**Migration numbers in the PRD are all stale** — OAuth work consumed 042-046, webhooks work is now at 047-048. Always check fresh before assigning a number.

## Standing facts (for the next dispatch wave)

- Factory Postgres: docker `ship-postgres-1` (:5433); `FACTORY_PG_CONTAINER=ship-postgres-1` for `worktree.sh`. `GH_REPO=troysatchell/ship` for all `gh` calls.
- **CodeRabbit is capacity-constrained this sprint** — try `gh pr comment <n> --body "@coderabbitai review"` once, wait ~90s; disclosed manual self-review substitutes if still limited. Occasionally clears and does a real review — check fresh each PR.
- **Only 3 checks are required for merge**: `typecheck · build · unit tests`, `source-code inventory`, `security scan (CodeQL)` (confirmed via `gh api repos/troysatchell/ship/branches/main/protection`). A separate non-required `CodeQL` (GitHub default Advanced Security) often flags unrelated things — check before spending time on it. `mergeStateStatus: UNSTABLE` (not `BLOCKED`) is safe to merge once the 3 required checks are green.
- **`gh pr checks` can be empty/stale for several seconds after a push** — cross-check `gh api repos/troysatchell/ship/commits/<full-sha>/check-runs` if it looks wrong.
- **`api/src/db/__tests__/migrations-042-043.test.ts`'s AC-2 fixture has an exclusion list that must be extended every time a new migration ALTERs (or FK-references) an oauth/webhooks table** — this has bitten TRO-421, TRO-425, and TRO-438 independently. The file's own header says to extend it; CI's coverage job catches this even when 8 rounds of local `gate.sh` don't (v8 coverage instrumentation changes test scheduling enough to surface it). Check this list proactively on any new migration ticket rather than waiting for CI to catch it.
- **Recurring agent failure mode across the whole session (~10+ occurrences):** a dispatched builder arms a background process for `gate.sh`/CI, then ends its turn waiting for a notification that never arrives. Always state this explicitly in dispatch briefs; still recurs — catch via `ListAgents` + `SendMessage`, don't wait indefinitely. Twice tonight this escalated to a genuine watchdog stall (system load spiked to ~28 during 3 concurrent `gate.sh` runs) — if multiple agents stall simultaneously, check `uptime` before assuming it's the self-report pattern again; it may be real resource contention, and resuming should still work either way.
- Lock the Linear ticket to In Progress BEFORE dispatching the Agent call, not after.
- Load-flake test identities keep recurring under concurrent `gate.sh` load (`weeks.test.ts`, `OrgChartPage.test.tsx`, others — rule-24 class in `lessons.md`) — always re-run standalone before believing a failure; never widen the quarantine.
- **Shared main-worktree collision (4-lane factory only), confirmed 3x this wave:** running `gate.sh`/merge-forward/scorecard-append *from the shared main checkout* (not a per-ticket `Ship-wt-tro_XXX` worktree) causes concurrent writers to stomp each other's uncommitted edits — hit `scorecard.jsonl`/`lessons.md` and this memory-bank (`activeContext.md`/`progress.md`, both full-file rewrites, not just appends). Confine ALL git mutations to your own ticket worktree; only `fetch`/`status`/`log`/`ls-remote` are safe in the shared main dir. Memory-bank files have no mitigation yet beyond notice-and-reapply — flagged to the chief orchestrator for a real fix.

## Open questions / carry-over (low priority)

- W6 deadline: **AM 2026-08-16** (brief's own AM/PM contradiction, planning for the earlier one).
- Carry-over, still untouched: PR #138 (TRO-350) merge-or-hold; TRO-353 inbox draft 404s; TRO-549/550/552 (Low/Medium, explicitly Backlog).
- TRO-593 (High) — `session-timeout.spec.ts`, 29/58 cases crash the browser context at a consistent 60s, suspected `page.clock` large-fast-forward mechanism — real pre-existing bug, not yet root-caused, worth real investigation time when there's room (unrelated to any W6 ticket, confirmed via bisection).
